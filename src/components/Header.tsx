export default function Header() {
  return (
    <div>
      <h1>React UI Elements Showcase</h1>
      <p>
        Explore common UI elements, organized by category. Each demo shows the
        element and its code.
      </p>
    </div>
  );
}
